Steps:
1) npm install
2) npm run dev
3) Open http://localhost:5173

Expected:
- Monaco’s gutter shows the actual line number plus an extra “1” overlay.
- Remove react-shiki usage in src/App.jsx and reload to confirm the ghost disappears.

Notes:
- react-shiki injects CSS at runtime (features.css) with `.line-numbers::before`.
- Monaco renders on the same page and uses `.line-numbers`, which collides with that
  global selector.
