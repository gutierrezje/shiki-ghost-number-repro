Steps:
1) npm install
2) npm run dev
3) Open http://localhost:5173

Expected:
- Each react-shiki gutter row shows the actual line number plus an extra “1” overlay.
- Comment out @import "tailwindcss"; in src/style.css and restart dev server; the ghost
  numbers disappear.
The code block uses the github-dark theme so the repro matches the dark UI.

Notes:
- react-shiki ships CSS for line numbers; we import it via `@import "react-shiki/css";`
  in src/style.css so Tailwind preflight is active alongside the line-number styles.
