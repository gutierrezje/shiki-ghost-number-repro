Steps:
1) npm install
2) npm run dev
3) Open http://localhost:5173

Expected:
- Each Monaco gutter row shows the actual line number plus an extra “1” overlay.
- Comment out @import "tailwindcss"; in src/style.css and restart dev server; the ghost
  numbers disappear.
